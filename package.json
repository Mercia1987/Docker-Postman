{
  "name": "docker-postman",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --host 0.0.0.0 --port 80",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MSS",
  "license": "ISC",
  "dependencies": {
    "json": "^11.0.0",
    "server": "^1.0.38"
  }
}
